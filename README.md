# ソーシャルゲーム ガチャ課題

PHPとMySQL(Docker)を使用したガチャシミュレーターです。

## 機能一覧
- 10連ガチャ機能（トランザクション処理済み）
- 結果履歴保存
- 所持アイテム一覧機能
- 排出率集計レポート
- モンスター配合（合成）機能
- 配合レシピ図鑑

## 環境構築・起動方法

1. Docker Desktopを起動します。
2. 以下のコマンドを実行します。
   ```bash
   docker-compose up -d --build
   http://localhost:8080/
